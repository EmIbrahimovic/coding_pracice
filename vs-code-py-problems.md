# py problems

importing file from parent directory:
1. manipulating the paths available to the current file (adding the parent folder, then just following that code, import the file you want to import) (two ways to do it sys path and Path; generally use absolute paths )
2. modify python path environment variable
3. turn the project into a package with `__init__` py files and a venv and other things.
