#include <iostream>
#include <string>
#include <vector>
#include "utils.h"

using namespace std;

class Solution {
public:
    List[List[int]] levelOrder(/* input needed */) {
        List[List[int]] result;

        return result;
    }
};

int main() {
    Solution sol;

    int tests[] = {

    };

    for (auto test : tests) {
        cout << sol.levelOrder(test) << endl;
    }

    return 0;
}
