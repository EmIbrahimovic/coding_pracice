#include <iostream>
#include <string>
#include <vector>
#include "utils.h"

using namespace std;

class Solution {
public:
    List[List[int]] mergeIntervals(/* input needed */) {
        List[List[int]] result;

        return result;
    }
};

int main() {
    Solution sol;
    /* input type + inputs needed */
    int tests[] = {

    };

    // Use utils if complex output
    for (auto test : tests) {
        cout << sol.mergeIntervals(test) << endl;
    }

    return 0;
}

