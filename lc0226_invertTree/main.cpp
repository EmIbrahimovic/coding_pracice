#include <iostream>
#include <string>
#include <vector>
#include "utils.h"

using namespace std;

class Solution {
public:
    Optional[TreeNode] invertTree(/* input needed */) {
        Optional[TreeNode] result;

        return result;
    }
};

int main() {
    Solution sol;

    int tests[] = {

    };

    for (auto test : tests) {
        cout << sol.invertTree(test) << endl;
    }

    return 0;
}
