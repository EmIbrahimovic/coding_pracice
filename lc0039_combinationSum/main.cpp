#include <iostream>
#include <string>
#include <vector>
#include "utils.h"

using namespace std;

class Solution {
public:
    List[List[int]] combinationSum(/* input needed */) {
        List[List[int]] result;

        return result;
    }
};

int main() {
    Solution sol;

    int tests[] = {

    };

    for (auto test : tests) {
        cout << sol.combinationSum(test) << endl;
    }

    return 0;
}
